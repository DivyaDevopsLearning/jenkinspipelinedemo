pipeline {
    agent any

    stages {
        stage('dev') {
            steps {
                echo 'Hello dev'
            }
        }
         stage('deploy') {
            steps {
                echo 'Hello deploy'
            }
        }
    }
}
